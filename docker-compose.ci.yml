services:
  mongo:
    # Compose merge behavior for `ports` can vary; publishing on a different
    # host port guarantees no conflict with local/dev Mongo on 27017.
    ports:
      - "27018:27017"

