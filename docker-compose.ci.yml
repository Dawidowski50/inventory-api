services:
  mongo:
    # CI/Jenkins: don't publish Mongo to host (avoid conflicts).
    ports: []

  api:
    # CI/Jenkins: publish API on alternate host port (avoid conflicts with dev on 8000).
    ports:
      - "8001:8000"

